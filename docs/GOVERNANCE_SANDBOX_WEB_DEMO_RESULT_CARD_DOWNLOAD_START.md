# GOVERNANCE_SANDBOX_WEB_DEMO_RESULT_CARD_DOWNLOAD_START

Use this note when governance-sandbox web-demo work needs the shortest stable browser-proof path:

1. import one scenario file,
2. submit one form,
3. render one result card,
4. verify one report download action from that same card,
5. widen the UI only after the narrow proof stays reproducible.

This keeps scenario input, result-card proof, and downloadable report evidence coupled in one small lane.
