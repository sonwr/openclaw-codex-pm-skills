# Governance sandbox scenario/report next-slice note

Keep each repo-1 PM handoff pinned to one imported governance scenario plus one regenerated JSON/Markdown/HTML report bundle, then name the smallest validated next slice without widening scope.
