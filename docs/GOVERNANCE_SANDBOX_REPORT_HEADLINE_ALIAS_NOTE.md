# GOVERNANCE_SANDBOX_REPORT_HEADLINE_ALIAS_NOTE

Treat `report.headline` as the same report-title input as `report.title` when a scenario file already owns the JSON/Markdown/HTML bundle.
Keep the wording short so the generated report heading, result card, and saved artifact names still read like one reviewable slice.
