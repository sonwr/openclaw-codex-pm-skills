# OpenClaw PM repo-4/repo-5 scenario report alias lane

Keep repo 5 phase one focused on one replayable slice: load one JSON/YAML scenario file, generate one JSON/Markdown/HTML report bundle, and allow a compact `report_bundle_alias` field to drive the basename when workshop exports need a shorter handoff label.

Validation gate: prove the alias with an automated test before any commit/push.
