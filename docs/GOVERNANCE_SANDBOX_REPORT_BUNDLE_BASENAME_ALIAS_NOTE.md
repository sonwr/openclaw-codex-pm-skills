# Governance Sandbox report bundle basename alias note

Use this note when you want a PM-facing reminder that `report.outputs.bundle_basename` is the explicit alias for naming one scenario-driven JSON/Markdown/HTML governance report bundle.
