# Governance sandbox scenario session bundle note

Treat `scenario_session_bundle` as a workshop-friendly wrapper alias for one imported proposal, stakeholder roster, and shared JSON/Markdown/HTML governance report bundle.

Keep that alias in the same PM proof lane as scenario-file input, named report artifacts, and small validated follow-up slices.
