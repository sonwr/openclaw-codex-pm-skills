# OpenClaw PM repo-4/repo-5 report output slug alias note

Use this when the current repo-4/repo-5 phase-one handoff needs one compact reminder that `report.output_slug` should keep the same governance-sandbox scenario-file -> report-bundle proof lane.

Keep the slice small:
- preserve repo 4 and repo 5 as the mandatory pair in every active run
- keep governance-sandbox scenario input plus markdown/html/json report proof coupled
- treat `report.output_slug` as a naming alias for the same validated report bundle before widening web-demo scope
