# Governance Sandbox scenario-report web demo lane note

Keep governance-sandbox pointed at the current delivery ladder: scenario-file input first, JSON/Markdown/HTML report bundle second, trait presets third, and web demo work only after those proof surfaces stay replayable.

When the workstream touches the future web demo, keep the first UI slice limited to one form, one primary action, and one result card that clearly echoes the imported scenario source plus generated report artifacts.

Use the same lane to carry UI/UX quality and Playwright-interactive stability rules forward: intro-first copy, one-screen form-to-card flow, deterministic selectors, stepwise verification, and failure recovery before widening the browser surface.
