# Governance Sandbox preset-map alias note

Use `preset_groups` or `stakeholder_preset_map` when one scenario fixture should keep a compact stakeholder-name -> preset mapping without expanding every object field first.
