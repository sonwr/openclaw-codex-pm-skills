# GOVERNANCE_SANDBOX_SCENARIO_NOTEBOOK_WRAPPER_NOTE

Use `scenario_notebook` or `scenario_notebook_bundle` when a governance rehearsal export already groups proposal, stakeholder, and report-bundle fields inside one notebook-style wrapper. Keep the same scenario-file -> JSON/Markdown/HTML report replay loop and rerun the validator before push.
