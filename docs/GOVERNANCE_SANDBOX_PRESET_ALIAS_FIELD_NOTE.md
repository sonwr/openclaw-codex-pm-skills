# Governance Sandbox preset alias field note

When repo 5 scenario files name a stakeholder preset through `preset_alias`, keep it equivalent to the existing preset lookup path so JSON/YAML scenario inputs can stay compact without losing the canonical preset key.
