# Governance Sandbox report output files card

Keep one scenario-file replay tied to one named JSON/Markdown/HTML report bundle.

Quick check:

1. confirm the scenario source path is visible,
2. confirm the report basename is visible,
3. confirm the generated `.json`, `.md`, and `.html` files all land in the same report directory.

Use this when you need a tiny reviewer-facing reminder that scenario-file input and report-output artifacts still move together in the phase-one delivery lane.
