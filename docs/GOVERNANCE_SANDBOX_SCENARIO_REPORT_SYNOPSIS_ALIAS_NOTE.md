# Governance sandbox scenario report synopsis alias note

Keep `report_synopsis` in the same smallest validated scenario-file -> JSON/Markdown/HTML report lane so memo-summary aliases stay tied to one replayable governance-sandbox bundle.
