# Governance Sandbox report output directory aliases note

When governance-sandbox scenario fixtures come from forms, PM briefs, or reviewer handoffs, the report bundle directory may be described as `report_dir`, `reports_dir`, `report_folder`, or `reports_folder`.

Keep the PM proof loop focused on one thing: one imported scenario should still regenerate one visible JSON, Markdown, and HTML report bundle without shell-only path glue.
