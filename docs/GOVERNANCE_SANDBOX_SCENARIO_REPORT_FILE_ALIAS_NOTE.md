# governance-sandbox scenario report file alias note

Keep repo-5 phase-one work focused on scenario-file input plus generated report artifacts.

When documenting the current slice, mention that scenario fixtures may point directly at named JSON, Markdown, and HTML report files so reviewers can replay one believable bundle without manual path glue.

Status line reminder: validate repo 5 before claiming commit/push in the five-repo report, and keep repo 4 plus repo 5 visible on every pass.
