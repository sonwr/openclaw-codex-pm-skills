# Governance sandbox report artifact paths

When a governance sandbox run emits JSON, markdown, and HTML outputs, keep the artifact paths visible in the report metadata so downstream automations can reuse the bundle without guessing filenames.
