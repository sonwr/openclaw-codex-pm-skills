Keep the PM handoff narrow: one stdin-fed JSON or YAML scenario replay, one regenerated markdown/html/json report bundle, and one validation rerun before any commit or push claim.

Use this when repo 5 phase-one work still starts with scenario input plus report artifacts, while repo 4 stays visibly active in the same pass.
