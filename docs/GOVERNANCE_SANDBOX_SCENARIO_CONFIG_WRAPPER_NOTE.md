# Governance Sandbox scenario_config wrapper note

Use `scenario_config` when a scenario file already arrives wrapped by a higher-level config object. Keep the replay story small: one imported proposal, preset-backed stakeholders, and one validated report bundle.
