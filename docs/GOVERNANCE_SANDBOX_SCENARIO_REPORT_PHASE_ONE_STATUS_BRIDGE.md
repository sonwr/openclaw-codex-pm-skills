# Governance-sandbox scenario/report phase-one status bridge

Keep repo-1 PM wording pointed at the same smallest validated repo-5 slice:

1. one JSON or YAML scenario file import,
2. one regenerated JSON/Markdown/HTML report bundle,
3. one short status line that only claims commit/push after validation passes.

Use this when the five-repo pass needs a compact reminder that repo 5 stays scenario-file/report-first while repo 4 and repo 5 both remain active every run.
