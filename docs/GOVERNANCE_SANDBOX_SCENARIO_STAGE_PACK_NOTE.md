# Governance Sandbox Scenario Stage Pack Note

Keep stage-oriented scenario exports small and replayable by allowing `scenario_stage_pack` / `scenario_stage_pack_bundle` to load the same proposal, stakeholder, and report bundle flow as other scenario-file wrappers.
