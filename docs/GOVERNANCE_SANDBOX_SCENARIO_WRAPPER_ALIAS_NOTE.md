# Governance Sandbox scenario wrapper alias note

Use this note when a reusable JSON/YAML fixture nests the real rehearsal under `scenario_payload`, `scenario_data`, or `scenario_bundle`. PM review should still treat it as the same proposal + stakeholder + report-bundle proof path, not as a separate authoring mode.
