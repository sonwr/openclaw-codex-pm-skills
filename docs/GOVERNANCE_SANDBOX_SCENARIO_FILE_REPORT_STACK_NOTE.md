# Governance Sandbox scenario-file report stack note

Keep the smallest believable Governance Sandbox progress tied to one imported JSON/YAML scenario file and one regenerated JSON/Markdown/HTML report stack.

Tiny PM loop:

1. replay the scenario file,
2. verify the report trio lands under one basename,
3. share only the validated artifact paths.
