# GOVERNANCE_SANDBOX_PROPOSAL_FILE_ALIAS_NOTE

Keep the phase-one governance-sandbox lane friendly to real scenario files: allow proposal content to come from a referenced text/markdown file inside the scenario bundle, then replay the same scenario into one JSON/Markdown/HTML report bundle before widening presets or web-demo scope.
