# Governance Sandbox scenario lab bundle note

Use the `scenario_lab_bundle` wrapper alias when a workshop or rehearsal export already packages one JSON/YAML scenario under a lab-style envelope.

Keep the same minimum proof loop:

1. import one scenario file,
2. generate one JSON/Markdown/HTML report bundle,
3. validate before push.
