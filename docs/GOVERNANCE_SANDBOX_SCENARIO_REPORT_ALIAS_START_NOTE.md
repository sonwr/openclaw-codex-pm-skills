# Governance Sandbox scenario/report alias start note

Keep the current delivery lane explicit: one imported scenario file should still produce one reviewer-visible JSON/Markdown/HTML report bundle before wider preset or demo work.

Fast pass:
1. Replay one JSON or YAML scenario fixture.
2. Confirm the generated bundle still exposes scenario source, basename, and artifact paths together.
3. Only then widen alias, preset, or web-demo scope.
