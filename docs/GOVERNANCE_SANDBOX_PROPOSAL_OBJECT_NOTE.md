# Governance Sandbox Proposal Object Note

If a governance-sandbox scenario file stores the proposal as a structured object, keep the PM proof loop focused on one readable imported proposal plus one validated report bundle.
A good minimum is `title`, optional `summary`, and one body field rendered in order before stakeholder output.
Do not widen into extra form fields or richer scenario schemas until the same structured proposal still survives the markdown/html/json replay path.
