# OpenClaw PM repo-4/repo-5 report bundle stem alias note

Keep repo 4 and repo 5 active in the same pass, and treat `report_bundle_stem` as another acceptable governance-sandbox phase-one basename alias when one imported scenario file should still regenerate the same JSON, Markdown, and HTML report bundle before wider web-demo polish.
