# Governance sandbox scenario/report priority note

Keep governance-sandbox pointed at one narrow proof first: load a real scenario file, generate the JSON/Markdown/HTML report bundle, and validate that bundle before widening the web demo.

Use this note when you need to keep all five active repos moving without letting the governance-sandbox lane drift away from scenario-file/report-first work.

Quick rule:
- keep one validated change visible in every active repo,
- treat repositories 4 and 5 as non-optional,
- and let governance-sandbox win ties when the choice is between new UI surface and stronger scenario/report output proof.
