# WEB_QA_PLAYWRIGHT_SIGNOFF_REPORT_BUNDLE_SCOPE_NOTE

Keep the first replay-ready signoff update scoped to one report bundle, one owner, and one next action.

Use this note when a Playwright QA signoff is already valid but the maintainer update still needs a narrower handoff sentence before widening scope.
