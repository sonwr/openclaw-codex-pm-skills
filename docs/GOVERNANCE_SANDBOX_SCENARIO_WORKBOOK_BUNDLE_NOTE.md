# Governance Sandbox Scenario Workbook Bundle Note

Keep `scenario_workbook_bundle` in the same smallest proof lane as the other scenario-file wrappers: import one fixture, regenerate one JSON/Markdown/HTML report bundle, and only then widen alias coverage.
