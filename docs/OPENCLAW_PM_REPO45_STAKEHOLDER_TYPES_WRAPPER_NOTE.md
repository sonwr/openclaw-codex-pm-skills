# OpenClaw PM repo45 stakeholder_types wrapper note

Keep repo 4 and repo 5 in the same validation-first micro-slice when governance-sandbox accepts wrapper-shaped scenario files that still expose stakeholder preset maps as `stakeholder_types`.

PM handoff rule:

- repo 5 proves the wrapped scenario import plus regenerated report bundle,
- repo 4 keeps the matching preset/result-card guidance visible,
- the five-line report still states validate/pass-or-hold status before any push claim.
