# governance-sandbox scenario/report phase-one status note

Keep the active governance-sandbox slice explicit in one short maintainer check:

- scenario file import stays first,
- markdown/html/json report output stays coupled to the same replay,
- repos 4 and 5 are never skipped in the five-repo loop,
- only validated changes are ready for push.
