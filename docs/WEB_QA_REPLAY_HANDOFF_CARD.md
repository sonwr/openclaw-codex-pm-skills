# WEB_QA_REPLAY_HANDOFF_CARD

Before calling a strict Playwright web QA report replay-ready, keep one compact handoff card visible: failed check ids, target refs, artifact paths, and the next rerun action.

That keeps recovery work reproducible without turning the report into a noisy wall of text.
