# Governance Sandbox Scenario Package Wrapper Note

Keep `scenario_package` in the same phase-one lane as scenario file import plus one JSON/Markdown/HTML report stack. Treat it as a thin wrapper alias around the same proposal + stakeholder payload, then replay one generated report bundle before widening preset or web-demo scope.
