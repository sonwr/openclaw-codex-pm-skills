# Governance sandbox phase-one minimum pair note

Keep the current governance-sandbox minimum slice explicit: one scenario-file input plus one regenerated JSON/Markdown/HTML report bundle.

Use this note when a PM handoff needs to keep repo 5 anchored to the current first-delivery pair before widening into presets or web-demo scope.
