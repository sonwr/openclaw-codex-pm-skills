# OpenClaw PM repo-4/repo-5 validate streak note

Keep repo 4 (`oss-launchpad-cli`) and repo 5 (`governance-sandbox`) in the same validate-first streak during each five-repo pass.

- Never skip repo 4 or repo 5.
- Prefer one small validated slice per repo over one large unverified batch.
- Keep governance-sandbox ordered as scenario file input first, then markdown/html/json report output, then presets, then web demo.
