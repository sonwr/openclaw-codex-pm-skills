# Repo 4/5 scenario source URI alias note

Keep repo 4 and repo 5 in the same validation-first pass when governance-sandbox scenario files expose their replay pointer as `scenario_source_uri`.

Use this alias only when it still resolves to one concrete scenario file and one regenerated JSON/Markdown/HTML report bundle before the five-line report.
