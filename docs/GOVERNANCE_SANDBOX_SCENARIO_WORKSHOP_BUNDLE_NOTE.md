# Governance Sandbox scenario workshop bundle note

Keep `scenario_workshop_bundle` in the same scenario-file-first lane as `scenario_lab` and `scenario_session_bundle` so workshop exports can replay one imported proposal plus one JSON/Markdown/HTML report bundle without manual reshaping.
