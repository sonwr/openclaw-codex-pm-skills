# Web QA Playwright report scope brief

Use this note when validator output is already clean enough to discuss, but the handoff must stay explicitly scoped to the report artifact, replay lane, and next proof target.

- Keep the claim artifact-scoped, not whole-run-scoped.
- Name the replay lane or report slice that is actually ready.
- Mention the next proof target before widening the handoff.
