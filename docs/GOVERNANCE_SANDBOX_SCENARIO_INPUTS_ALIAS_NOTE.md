# Governance Sandbox scenario-inputs alias note

Keep `scenario_inputs` available as a compact scenario-file wrapper when proposal, stakeholder, and report metadata are imported together. Use it when a PM-facing fixture should read like one scenario handoff without adding another nesting layer.
