# governance-sandbox scenario source short alias note

Keep scenario-file -> report-bundle proof compact when imported governance-sandbox fixtures label the original scenario source as `scenario_src` or `source_href`; the report bundle should still expose the same scenario source path or URL for replay handoff.
