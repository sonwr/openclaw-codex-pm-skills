# Governance Sandbox report artifacts_root alias note

Use `artifacts_root` as a compact scenario-file alias for the directory that should receive the generated JSON, Markdown, and HTML governance report bundle.

Keep this alias in the same scenario-file-first lane as `report_output_directory` and `report_folder`: one imported JSON/YAML scenario should still regenerate one reviewable report bundle before preset or web-demo expansion.
