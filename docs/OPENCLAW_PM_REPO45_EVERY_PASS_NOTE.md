# OpenClaw PM repo-4/repo-5 every-pass note

Treat repo 4 (`oss-launchpad-cli`) and repo 5 (`governance-sandbox`) as a locked pair in every five-repo pass.

- Check both repos each run.
- Land one validated micro-step in both when possible.
- If either repo is blocked, keep the short report explicit about the hold reason instead of silently skipping it.
