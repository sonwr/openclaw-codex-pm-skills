# Governance Sandbox report.output_label alias note

When governance-sandbox scenario files need one short reviewer-facing name for the generated JSON/Markdown/HTML bundle, `report.output_label` can act as the same bundle-basename input as the existing basename/slug aliases. Keep it in the same small proof lane: one imported scenario file, one generated report bundle, one passing validation command before push.
