# OPENCLAW_PM_REPO45_PHASE_ONE_OUTPUT_DIR_ALIAS_NOTE

Keep repo 4 and repo 5 aligned on one tiny validated phase-one slice:

- repo 4 (`oss-launchpad-cli`) stays visibly active on one preset/result-card proof step,
- repo 5 (`governance-sandbox`) keeps one JSON/YAML scenario file tied to one regenerated JSON/Markdown/HTML report bundle,
- `report_output_dir` should be treated as the same bundle-location handoff as `report_dir` before widening web-demo scope,
- re-run validation before any commit/push claim,
- keep the user-facing cron report to exactly one short line per repo.
