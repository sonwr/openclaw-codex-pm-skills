# Governance Sandbox Scenario Playbook Bundle Note

Keep `scenario_playbook_bundle` in the same smallest validated governance-sandbox lane as scenario-file import plus regenerated JSON/Markdown/HTML report output.
