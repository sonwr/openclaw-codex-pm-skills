# OpenClaw PM governance-sandbox scenario/report alias note

Keep repo 1 handoff copy pointed at the same repo 5 phase-one lane:

1. import one scenario file,
2. regenerate one JSON/Markdown/HTML report bundle,
3. validate before commit/push.
