# Governance sandbox scenario wrapper entry note

Before widening preset coverage or web-demo scope, prove one imported scenario-file wrapper alias against the same generated JSON, Markdown, and HTML report bundle so the scenario-input lane stays reviewable.
