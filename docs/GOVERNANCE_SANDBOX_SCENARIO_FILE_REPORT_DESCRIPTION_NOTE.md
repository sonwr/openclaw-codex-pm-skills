# Governance Sandbox scenario-file report description note

Keep one imported JSON or YAML scenario tied to one generated report bundle that already shows the memo description in Markdown and HTML.

Use this when you need a small PM-facing replay for scenario-file input plus human-readable report framing without widening scope beyond the current report bundle lane.
