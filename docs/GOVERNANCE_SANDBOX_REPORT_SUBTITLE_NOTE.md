# Governance Sandbox Report Subtitle Note

Keep one short reviewer-facing subtitle under the main report title when a scenario bundle needs a clearer handoff cue.
Use scenario-file metadata so the same subtitle survives JSON, Markdown, and HTML report exports without extra copy edits.
