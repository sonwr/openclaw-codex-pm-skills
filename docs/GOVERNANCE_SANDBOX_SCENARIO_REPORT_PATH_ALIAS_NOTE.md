# Governance sandbox scenario report path alias note

Use this when a `governance-sandbox` scenario file should carry report output paths directly instead of repeating `--report-json`, `--report-markdown`, and `--report-html` in every handoff.

Keep the handoff narrow:

- one scenario file,
- one output path set,
- one reproducible replay command.
