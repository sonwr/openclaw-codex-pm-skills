# OpenClaw PM repo-4/repo-5 JSON/YAML report-ref alias note

Keep repo 5 on the same phase-one lane: one imported JSON/YAML scenario file should still regenerate one JSON/Markdown/HTML report bundle even when the basename arrives as `report_bundle_ref`.
Keep repo 4 active in the same pass, but do not widen the web-demo/result-card scope until that scenario-file + report-bundle replay is green again.
