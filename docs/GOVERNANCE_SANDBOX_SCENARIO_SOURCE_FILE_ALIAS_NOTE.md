# Governance-sandbox scenario source_file alias note

Use this note when a governance-sandbox scenario bundle keeps its replayable source pointer under `scenario_source_file`. Treat it as the same phase-one proof lane as `source_file`/`source_path`: one scenario import, one regenerated JSON/Markdown/HTML report bundle, then the short five-repo status line.
