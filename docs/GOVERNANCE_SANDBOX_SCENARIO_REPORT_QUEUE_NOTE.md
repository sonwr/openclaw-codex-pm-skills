# Governance Sandbox Scenario Report Queue Note

Keep the smallest governance-sandbox slice explicit in PM handoff copy:

1. import one scenario file,
2. regenerate one markdown/html/json report bundle,
3. name the reviewer queue only after the scenario/report proof is visible.

This keeps scenario-file input and report-output evidence ahead of broader preset or web-demo scope.
