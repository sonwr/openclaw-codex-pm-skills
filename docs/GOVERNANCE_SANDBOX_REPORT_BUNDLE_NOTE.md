# Governance sandbox report bundle note

Use this note when a PM skill hands governance-sandbox output to another reviewer or ACP follow-up.

- Keep the handoff scoped to one scenario file and one shared JSON/Markdown/HTML report bundle.
- Surface the artifact paths explicitly so the next operator does not guess filenames.
- Keep the follow-up prompt tied to one owner-visible next action.
