# OpenClaw PM five-repo validate loop note

Keep every active five-repo pass narrow: land one small validated improvement per repo, keep repo 4 (`oss-launchpad-cli`) and repo 5 (`governance-sandbox`) mandatory, and do not widen handoff wording until the local proof command has passed.
