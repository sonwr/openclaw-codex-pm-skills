# Governance Sandbox Stakeholder List Alias Note

Keep scenario-file intake flexible for workshop fixtures that describe participants as `stakeholder_list` or `stakeholder_entries`.
Treat those aliases as the same first-phase scenario import lane so one JSON/YAML file still feeds the same markdown/html/json report proof bundle.
