# OpenClaw PM repo45 proposal input markdown path note

Keep repo 4 and repo 5 aligned around one small validated proof lane:
- repo 5 (`governance-sandbox`) may load proposal body text from `proposal_input_markdown_path` inside one JSON/YAML scenario file,
- the same replay should still regenerate the JSON/Markdown/HTML report bundle,
- repo 4 stays on one validator-backed web-app/result-card support slice in the same pass,
- finish with the exact five-line report after the validation commands pass.
