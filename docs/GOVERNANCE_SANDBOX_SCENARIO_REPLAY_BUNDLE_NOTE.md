# Governance sandbox scenario replay bundle note

Keep scenario-file work scoped to one imported fixture plus one regenerated JSON/Markdown/HTML report bundle.

## Quick check
- Replay one JSON or YAML scenario file.
- Confirm the same run exposes the scenario source and the regenerated report bundle paths.
