# OpenClaw PM five-repo phase-one status line note

Keep the five-repo loop honest and small:

- state the active repo name,
- say whether a real change landed,
- name the validation command or pass/fail result,
- and only claim commit/push after validation clears.

When `governance-sandbox` is still in phase one, make sure the status wording keeps scenario-file input plus markdown/html/json report output visible as the priority slice.
