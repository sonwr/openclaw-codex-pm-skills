# Governance-sandbox report result-card start note

Use this PM handoff when governance-sandbox work needs the shortest proof between scenario input and reviewer-visible report output.

Keep the proof lane deterministic:

- import one scenario file
- render one result card
- expose one report artifact path
- verify one report download action

This keeps UI/UX review and Playwright-style checkpoint recovery scoped to one believable slice.
