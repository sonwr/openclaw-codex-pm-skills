# Governance Sandbox report basename alias cue

Keep one imported scenario tied to one generated report basename before widening demo scope. Prefer a single basename-driving field per fixture so JSON/Markdown/HTML artifacts stay obviously related in review.
