# Governance sandbox web demo UI scope brief

Keep the first governance-sandbox web demo intentionally narrow: one scenario input form, one visible result card, and one report-download checkpoint.

Use this brief when UI copy starts expanding faster than reproducible proof. The slice is ready to widen only after the same scenario replay still produces the same report artifact paths and the same browser-visible result summary.
