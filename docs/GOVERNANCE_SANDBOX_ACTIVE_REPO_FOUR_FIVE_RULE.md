# Governance sandbox active repo 4/5 rule

Keep all five repos active, but never let repo 4 (`oss-launchpad-cli`) or repo 5 (`governance-sandbox`) disappear from a pass.

Small-slice rule:
- touch all five repos with one honest status check,
- land at least one validated improvement in repo 4,
- land at least one validated improvement in repo 5,
- keep repo 5 ordered as scenario file -> report bundle -> presets -> web demo -> demo GIF.
