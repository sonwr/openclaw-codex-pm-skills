# Repo 4/5 priority gate

Keep repository 4 (`oss-launchpad-cli`) and repository 5 (`governance-sandbox`) in every improvement loop.
Validate each repo before commit/push, and keep governance-sandbox phase-one work anchored on scenario file inputs plus markdown/html report outputs.
