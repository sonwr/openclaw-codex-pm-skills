# Governance Sandbox report_folder alias note

If governance-sandbox scenarios use `report_folder`, keep the PM handoff tied to one imported scenario and one generated JSON/Markdown/HTML report bundle.
The alias is only useful when reviewers can still see where the bundle landed without reopening the CLI code.
