# Governance Sandbox Scenario Studio Note

Keep design-studio exports in the same phase-one lane as every other governance-sandbox scenario file.

- Accept `scenario_studio` and `scenario_studio_bundle` as import wrappers when the payload still contains one proposal plus one stakeholder set.
- Keep the proof loop the same: one imported scenario file should still be able to regenerate one JSON/Markdown/HTML report bundle.
- Treat this as scenario-file/report-first work, not as permission to widen the web demo scope.
