# Governance sandbox scenario report fixture proof note

Keep one validated governance-sandbox replay tied to the exact scenario fixture path, the saved bundle basename, and the regenerated JSON / Markdown / HTML artifact trio before widening the five-repo pass.
