# Governance Sandbox Trait Preset Bundle Queue Note

Keep the first stakeholder trait preset bundle limited to `dao`, `delegates`, `contributors`, `investors`, and `community`, and queue it behind scenario-file input plus markdown/html/json report proof.
