# Governance Sandbox scenario journal wrapper note

Keep repo-5 phase-one scope small: when a governance export already wraps the proposal, stakeholders, and report metadata under `scenario_journal` or `scenario_journal_bundle`, treat it as the same scenario-file -> JSON/Markdown/HTML report bundle proof lane.

Validate the wrapper import first, then keep commit/push language tied to a passing proof loop.
