# GOVERNANCE_SANDBOX_DAO_REPORT_EXAMPLE_NOTE

Use this when reviewers want one concrete governance-sandbox example that covers scenario-file input, preset-backed stakeholders, and a named JSON/Markdown/HTML report bundle.

## Fast cue

Prefer a single DAO-flavored scenario example that already demonstrates:

- scenario-file driven proposal input,
- preset-backed stakeholder loading,
- and one reusable report basename across JSON, markdown, and HTML outputs.

## Pass condition

A reviewer can point to one example fixture and one report directory command without rebuilding the proof path from multiple docs.
