# Governance sandbox report metadata readability note

Keep scenario name, generated-at time, scenario source, and JSON/Markdown/HTML artifact paths readable in one pass so a human can reopen the bundle without guessing.
