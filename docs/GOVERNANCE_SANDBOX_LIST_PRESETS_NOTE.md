# Governance Sandbox list-presets note

Run `PYTHONPATH=src python3 -m governance_sandbox.cli run --list-presets` before editing scenario fixtures or web-demo forms so the supported stakeholder trait presets stay explicit (`dao`, `delegates`, `contributors`, `investors`, `community`).
