# governance-sandbox proposal_copy_markdown alias note

When governance-sandbox grows scenario-file aliases, keep `proposal_copy_markdown` inside the same scenario-file plus markdown/html report proof lane. Validate that one alias and one report bundle before widening into broader web-demo scope.
