# Governance phase-one replay lane

Keep repo 5 focused on one replayable path:

1. load one JSON/YAML scenario file,
2. regenerate one JSON/Markdown/HTML report bundle,
3. rerun the validator before push.

Use this note when repo 1 handoff wording needs a compact reminder that scenario-file support and report generation still outrank preset or demo polish.
