# Governance Sandbox phase-one loop

Keep the next governance-sandbox slice small and reviewer-visible:

1. import one real JSON or YAML scenario file,
2. regenerate one JSON/Markdown/HTML report bundle,
3. rerun the matching proof command before handoff.

This note exists to keep phase-one work locked to scenario-file input and report outputs before preset expansion or web demo scope.
