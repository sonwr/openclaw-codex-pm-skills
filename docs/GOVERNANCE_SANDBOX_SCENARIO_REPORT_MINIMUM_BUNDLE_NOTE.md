# governance-sandbox scenario report minimum bundle note

Use one scenario file and one report directory as the minimum proof slice.

Keep the first pass limited to:

- one JSON or YAML scenario file,
- one generated JSON result,
- one generated markdown report,
- one generated HTML report.

That bundle is the smallest believable proof that scenario import and report export are both wired correctly before widening into richer UI or replay flows.
