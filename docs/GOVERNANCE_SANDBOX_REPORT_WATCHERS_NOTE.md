# GOVERNANCE_SANDBOX_REPORT_WATCHERS_NOTE

Keep one imported scenario tied to a clear report-watchers audience when the memo bundle needs a lightweight reviewer handoff.
Use `report.watchers` only as an audience-style alias; keep the same JSON/Markdown/HTML report bundle proof path.
