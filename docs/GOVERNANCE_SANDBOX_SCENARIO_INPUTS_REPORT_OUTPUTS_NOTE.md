# Governance Sandbox Scenario Inputs + Report Outputs Note

Keep the smallest believable governance-sandbox replay scoped to one imported scenario payload under `inputs` and one visible report-output basename under `inputs.report.outputs`.

PM rule: prove that the same fixture still owns proposal input, stakeholder input, and the generated JSON/Markdown/HTML bundle naming before widening aliases, presets, or web-demo work.
