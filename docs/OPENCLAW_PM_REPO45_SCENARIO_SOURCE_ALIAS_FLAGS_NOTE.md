# repo-4/repo-5 scenario-source alias flags note

Keep repo 4 and repo 5 aligned on one validator-backed phase-one slice: repo 4 can scaffold the first governance web form lane, while repo 5 accepts `--proposal-file` / `--scenario-source` as the same JSON-or-YAML scenario replay entry before regenerating the markdown/html/json report bundle.
