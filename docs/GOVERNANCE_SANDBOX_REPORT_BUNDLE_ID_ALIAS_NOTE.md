# Governance Sandbox report_bundle_id alias note

Treat `report_bundle_id` as the same phase-one report-bundle basename alias used by governance-sandbox scenario files.
Keep the validation loop small: replay one scenario file, confirm the regenerated JSON/Markdown/HTML bundle, then write the short repo status line.
