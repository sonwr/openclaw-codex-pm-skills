# OpenClaw PM five-repo status-line order gate

Keep the recurring five-repo report auditable: keep the repository order stable, keep each repo to one short line, and make the validation result plus commit/push state visible on that same line.
