# Governance Sandbox scenario input wrapper note

Keep scenario-file wrapper guidance small: `scenario_document` and `scenario_manifest` should both reopen one proposal + stakeholder payload without expanding scope beyond one replayable input lane.
