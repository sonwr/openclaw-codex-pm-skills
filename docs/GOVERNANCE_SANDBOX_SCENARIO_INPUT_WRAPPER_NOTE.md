# GOVERNANCE_SANDBOX_SCENARIO_INPUT_WRAPPER_NOTE

Keep the first governance-sandbox scenario-file replay flexible: `scenario_input`, `scenario_document`, and `scenario_manifest` should all be valid wrapper names when the payload still carries one proposal, one stakeholder set, and one report handoff.
