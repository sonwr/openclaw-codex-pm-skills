# Governance Sandbox report reviewer alias note

Use `report.reviewer` (singular) when a scenario fixture names one primary memo reader, but keep the rendered output aligned with the same `Report audience` field used by plural reviewer/audience aliases in JSON, Markdown, and HTML artifacts.
