# Governance Sandbox Proposal File Direct Alias Note

Use `proposal_file`, `proposal_path`, or `proposal_src` when a scenario file should load proposal copy from a local markdown/text file before generating the same JSON/Markdown/HTML report bundle. Keep the replay small: one imported proposal file, one stakeholder roster, one validated report bundle.
