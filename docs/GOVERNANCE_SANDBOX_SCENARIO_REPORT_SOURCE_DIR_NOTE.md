# Governance-sandbox scenario/report source-dir note

Use one imported scenario file and one generated report directory in the same replay note.

Keep the scenario source path visible first, then name the report directory that holds the JSON/Markdown/HTML bundle.
