# Governance Sandbox Scenario JSONC Report Start Note

Treat JSONC scenario files as a small, validated expansion of scenario-file input support.
Keep the first proof loop tight: load one commented scenario file, produce JSON output, and emit markdown/html report artifacts from the same run before widening demo scope.
