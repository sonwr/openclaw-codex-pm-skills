# Governance Sandbox scenario -> report artifact note

Keep the PM proof loop small:

- one scenario file
- one generated markdown artifact
- one generated html artifact
- one matching JSON artifact path

Do not widen the slice until the same scenario replay can regenerate the same report bundle with stable names.
