# governance-sandbox scenario demo wrapper note

Keep the smallest web-demo rehearsal wrapper honest before widening the browser surface.

- Prefer `scenario_demo` when one imported scenario should replay through the same JSON / Markdown / HTML report bundle path as the broader demo packs.
- Treat it as a narrow alias for the first believable demo handoff, not a separate execution mode.
- Re-run the governance-sandbox scenario-file proof before widening any UI copy or browser automation notes.
