# Governance sandbox scenario JSON5 report note

Keep phase-one progress centered on scenario-file input plus report generation:

- accept `.json5` alongside `.json`, `.jsonc`, and `.yaml` when the same scenario payload shape already works,
- keep one reproducible CLI replay that proves markdown/html/json report artifacts still land together,
- treat JSON5 as a compatibility edge for maintainers who want comments and trailing-comma-friendly scenario fixtures without leaving the scenario-file lane.
