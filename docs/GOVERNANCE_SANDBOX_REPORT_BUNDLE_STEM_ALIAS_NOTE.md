# Governance sandbox report bundle stem alias note

When governance-sandbox scenario files need a shorter report-naming key, prefer `report.bundle_stem` or `report.outputs.bundle_stem` and keep that same slug tied to the generated JSON / Markdown / HTML bundle during validation.
