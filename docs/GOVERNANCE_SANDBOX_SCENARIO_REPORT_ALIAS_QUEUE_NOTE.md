# Governance Sandbox scenario/report alias queue note

Keep small PM slices focused on one validated scenario-file alias plus one regenerated JSON/Markdown/HTML report bundle before widening fixture breadth.
