# Governance Sandbox Scenario File + Report Priority Stack

Keep the minimum acceptable governance-sandbox progress focused on one validated chain:

1. Load the proposal plus stakeholders from a JSON/YAML scenario file.
2. Produce JSON, markdown, and HTML report artifacts from the same run.
3. Surface report priority wording only after the scenario-to-report path is still reproducible.

Do not expand into broader demo polish until the scenario-file import and report bundle stay green together.
