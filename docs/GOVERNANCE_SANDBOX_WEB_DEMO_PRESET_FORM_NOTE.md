# Governance Sandbox Web Demo Preset Form Note

Keep the first web-demo form wired to the exported preset catalog before widening the UI surface.

- Reuse one machine-readable preset source instead of duplicating labels.
- Show the preset name and summary together in the first selectable form state.
- Verify the same preset path still feeds CLI, scenario files, and the first browser proof.
