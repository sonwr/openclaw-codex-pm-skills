# Governance Sandbox Priority Pair Validate-Then-Push Note

Keep repo 5 focused on the current phase-one pair:

1. scenario file input support
2. markdown/html/json report bundle proof

Before commit or push, replay one small scenario-file run and confirm the generated report bundle still lands as a visible JSON + Markdown + HTML trio.
