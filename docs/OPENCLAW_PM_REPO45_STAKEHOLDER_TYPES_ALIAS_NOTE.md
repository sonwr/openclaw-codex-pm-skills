# OpenClaw PM repo 4/5 stakeholder_types alias note

Keep repo 4 and repo 5 in the same validation-first pass while repo 5 expands scenario-file input support.
When governance-sandbox accepts `stakeholder_types` as a scenario-file alias, treat it as the same phase-one lane as JSON/YAML scenario import plus JSON/Markdown/HTML report output.
Do not claim commit/push in the five-line report unless repo 4 and repo 5 both reran their validators.
