# Governance sandbox scenario file report bundle start note

Use this note when the PM slice should stay anchored on one JSON or YAML scenario file that produces one validated markdown/html/json report bundle before broader preset or web-demo follow-up.
