# Governance sandbox scenario/report bundle review note

When a QA or PM workflow depends on `governance-sandbox`, keep the first proof scoped to a JSON/YAML scenario file plus a generated markdown/html/json report bundle.

Review loop:
- confirm the scenario fixture is runnable from the CLI
- confirm report artifacts land in the expected bundle directory
- confirm markdown/html outputs stay reviewable before adding wider automation
