# GOVERNANCE_SANDBOX_REPORT_BUNDLE_NAME_ALIAS_NOTE

Use `report_bundle_name` when you want one scenario-file field to lock the shared JSON, Markdown, and HTML report basename for `governance-sandbox`.
