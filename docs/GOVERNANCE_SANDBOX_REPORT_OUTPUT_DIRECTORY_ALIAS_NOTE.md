# Governance Sandbox report output directory alias note

Use `report_output_directory` when one top-level scenario fixture should drive the default JSON, Markdown, and HTML bundle directory without nesting a separate `report` block.
