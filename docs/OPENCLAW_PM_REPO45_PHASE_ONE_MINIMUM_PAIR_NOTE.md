# Repo 4/5 phase-one minimum pair note

Keep repo 4 and repo 5 visibly active in the same five-repo pass, but treat repo 5 as the stricter phase-one gate:

1. replay one JSON/YAML scenario file in `governance-sandbox`
2. regenerate one JSON/Markdown/HTML report bundle
3. land one small validator-backed repo-4 follow-up in the same pass

Do not claim commit or push for either repo until the local validation rerun for that repo is green.
