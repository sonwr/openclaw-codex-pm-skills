# Governance Sandbox web demo report download note

Keep the first governance-sandbox web demo scoped to one scenario form, one result card, and one believable report-download proof.

Quick check:
- submit one scenario fixture,
- confirm the result card exposes the named JSON/Markdown/HTML bundle,
- reopen one downloaded artifact before widening the UI.
