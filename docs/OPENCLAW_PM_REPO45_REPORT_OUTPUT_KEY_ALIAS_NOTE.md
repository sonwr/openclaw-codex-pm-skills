# OPENCLAW_PM_REPO45_REPORT_OUTPUT_KEY_ALIAS_NOTE

Keep repo 4 and repo 5 aligned on one validate-before-push micro-slice: if governance-sandbox exposes the shared report basename as `report_output_key` or nested `report.outputs.output_key`, keep the repo-4 handoff wording on the same single result-card/report-key proof lane and rerun the smallest local validators before commit/push.
