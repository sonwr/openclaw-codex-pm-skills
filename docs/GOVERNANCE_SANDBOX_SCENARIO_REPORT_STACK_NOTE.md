# Governance Sandbox scenario report stack note

Use this note when you only need to confirm one thing: a scenario-file run produced the minimum report stack that a PM can review quickly.

Keep the check compact:
- confirm one scenario file was the input
- confirm the generated stack includes JSON + Markdown + HTML
- confirm the bundle is ready for a short PM-facing handoff
