# Governance Sandbox web demo result-card handoff

When `governance-sandbox` moves into the simple web demo phase, keep one stable proof chain:

1. load one scenario file,
2. render one result card,
3. save the matching report bundle,
4. validate the same UI/report pair before sharing status.

This keeps UI work, report generation, and Playwright-style reproducibility tied to the same small slice.
