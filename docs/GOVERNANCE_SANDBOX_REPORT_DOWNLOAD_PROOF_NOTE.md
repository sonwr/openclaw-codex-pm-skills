# Governance-sandbox report download proof note

When a governance-sandbox workstream touches the first web demo, keep the proof sequence narrow:

1. replay one scenario file,
2. confirm one visible result card,
3. confirm the report bundle paths are still exposed for download or handoff,
4. only then widen the browser-proof surface.

Use this note when PM wording needs to stay coupled to scenario input, reviewer-visible output, and one reproducible report bundle.
