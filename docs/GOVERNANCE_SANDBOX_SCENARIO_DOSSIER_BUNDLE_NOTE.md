# Governance Sandbox Scenario Dossier Bundle Note

Keep dossier-style imports small and explicit: `scenario_dossier` and `scenario_dossier_bundle` should still replay through the same scenario-file -> JSON/Markdown/HTML report proof lane before widening fixture scope.
