Keep `stakeholder_trait_map` / `trait_map` in the same proof lane as one scenario file and one reproducible JSON/Markdown/HTML report bundle.
Use the alias only when it shortens scenario authoring without hiding the imported scenario artifact or the generated report basename.
