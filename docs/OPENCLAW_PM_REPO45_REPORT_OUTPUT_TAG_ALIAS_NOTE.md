# OpenClaw PM repo-4/repo-5 report-output-tag alias note

Keep the repo-4/repo-5 phase-one handoff small: one repo-4 validator-backed UI/result-card slice plus one repo-5 scenario-file replay that may carry the shared JSON/Markdown/HTML bundle basename as `report_output_tag`.

Before any commit or push claim, rerun the smallest validator path that proves the same scenario import still regenerates the bundle cleanly.
