# Governance sandbox scenario record wrapper note

Use `scenario_record` when governance-sandbox fixtures arrive as archive-style replay records instead of plain top-level scenario files.
Keep the same PM proof rule: one imported wrapper should still regenerate the JSON, Markdown, and HTML governance report bundle without extra shell glue.
