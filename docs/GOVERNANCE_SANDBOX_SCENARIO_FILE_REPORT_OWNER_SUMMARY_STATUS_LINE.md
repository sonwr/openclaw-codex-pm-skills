# Governance Sandbox scenario-file report owner+summary status line

Use this note when one imported scenario file already renders both visible report owner metadata and a short report summary inside the generated JSON/Markdown/HTML bundle, and you only need a one-line maintainer update.

Status line pattern:

- `scenario file + owner/summary metadata + JSON/Markdown/HTML bundle revalidated`
