# Governance Sandbox Scenario Source + Basename Note

Keep one imported scenario source path and one generated report bundle basename visible in the same handoff line so reviewers can replay the exact JSON/Markdown/HTML report bundle without guessing which scenario produced it.
