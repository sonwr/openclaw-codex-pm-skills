# Governance Sandbox Report Synopsis Alias

Use `report.synopsis` when one scenario file should carry a short reviewer-facing memo summary into the generated JSON, Markdown, and HTML report bundle without duplicating copy elsewhere.
