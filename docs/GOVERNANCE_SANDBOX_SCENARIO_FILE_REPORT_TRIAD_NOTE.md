# Governance Sandbox Scenario File Report Triad Note

Keep the first scenario-file proof narrow and reproducible:

1. Start from one `--scenario-file` input.
2. Emit the full report triad (`.json`, `.md`, `.html`) from the same run.
3. Reopen the generated bundle before widening scope to UI/demo work.

This keeps scenario parsing, report rendering, and artifact paths aligned in one checkpoint.
