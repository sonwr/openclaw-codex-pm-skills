# Governance-sandbox source_location alias note

Keep repo-1 PM guidance aligned with repo-5 scenario-file work: `source_location` is a neutral scenario-source alias for imported JSON/YAML fixtures, and it should flow into the same report metadata lane as `scenario_src` and `source_href` before widening preset or web-demo scope.
