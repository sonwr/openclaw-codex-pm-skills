# Governance Sandbox report output slug alias note

Keep scenario-file report bundles easy to rename from fixtures: `report.output_slug` and `report.outputs.output_slug` should resolve to the same bundle basename lane as the existing report basename aliases.

Use this when a maintainer wants human-friendly fixture fields that still replay to one shared JSON/Markdown/HTML report stack.
