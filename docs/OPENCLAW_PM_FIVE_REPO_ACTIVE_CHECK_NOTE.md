# OpenClaw PM five-repo active check note

Keep each five-repo pass visibly active: make one small validated improvement per repo, never skip repos 4 or 5, and keep the user-facing report to one short line per repository.
