# Governance-sandbox nested inputs report note

If a scenario fixture already uses `inputs`, keep report metadata replay-friendly in the same bundle by allowing `inputs.report` to carry the memo title, summary, audience, basename, and output directory.

Use this when PM handoff wants one scenario file that keeps proposal text, stakeholder import, and report-output naming together without pushing report fields back to the top level.
