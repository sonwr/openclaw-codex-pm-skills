# Governance Sandbox report_bundle_tag alias note

Keep `report_bundle_tag` treated as a stable report bundle basename alias so one imported scenario file still regenerates the same JSON, Markdown, and HTML report bundle before widening preset or demo scope.
