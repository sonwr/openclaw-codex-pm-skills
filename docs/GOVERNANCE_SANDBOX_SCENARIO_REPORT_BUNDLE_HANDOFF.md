# Governance Sandbox scenario report bundle handoff

Keep one PM handoff sentence tied to the same imported scenario file, report bundle basename, and generated JSON / Markdown / HTML artifacts.

Quick check:

1. confirm the scenario file path is explicit,
2. confirm the report basename is the same across JSON / Markdown / HTML,
3. confirm the handoff sentence names the bundle before widening scope.
