# PM validate-then-push active repos note

Keep every active repository loop honest: make one small improvement, run the narrowest local validation that proves the change, then commit and push only the repos that passed.
