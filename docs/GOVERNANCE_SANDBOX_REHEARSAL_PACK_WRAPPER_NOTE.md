# GOVERNANCE_SANDBOX_REHEARSAL_PACK_WRAPPER_NOTE

Use `scenario_rehearsal_pack` when repo 5 needs one more JSON/YAML scenario wrapper that still keeps the proposal + stakeholder payload obvious.

Keep the first win small: scenario-file input should still land in the same JSON/Markdown/HTML report bundle, and repo 4 plus repo 5 should stay visible in the same validated progress pass.
