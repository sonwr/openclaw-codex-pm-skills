# Governance Sandbox Repo 4/5 Scenario-Report-First Note

Keep repo 4 and repo 5 active in the same pass.

- Repo 4 (`oss-launchpad-cli`) stays active when the `web-app` preset JSON, result-card scope, and validation path stay visible.
- Repo 5 (`governance-sandbox`) should keep phase one anchored on scenario-file input plus Markdown/HTML report generation before broader preset/demo work.
- Short PM status is only complete when both repos appear in the same validated loop.
