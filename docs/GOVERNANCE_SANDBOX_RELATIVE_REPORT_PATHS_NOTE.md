# Governance Sandbox relative report paths note

Use relative `--report-json`, `--report-markdown`, and `--report-html` paths from the scenario-file directory so replayable fixtures keep report artifacts next to the imported JSON/YAML source.
