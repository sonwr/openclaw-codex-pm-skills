# governance-sandbox scenario manifest bundle note

Keep scenario-file work centered on one replayable import path: when a fixture arrives as `scenario_manifest_bundle` or `scenario_packet_bundle`, treat it as the same scenario-file-first lane and keep the generated JSON/Markdown/HTML report bundle attached to that one source fixture.
