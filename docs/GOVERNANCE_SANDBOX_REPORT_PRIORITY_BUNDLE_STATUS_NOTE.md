# Governance-sandbox report priority bundle status note

Keep each governance-sandbox slice scoped to one imported scenario file plus one generated JSON/Markdown/HTML report bundle, and write the status update in that same order: scenario source, artifact bundle, then validation state.
