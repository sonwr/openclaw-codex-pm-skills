# Governance Sandbox scenario_lab wrapper note

Use `scenario_lab` as a scenario-file wrapper alias when you want to hand off a proposal + stakeholder bundle without changing the inner governance payload shape.
