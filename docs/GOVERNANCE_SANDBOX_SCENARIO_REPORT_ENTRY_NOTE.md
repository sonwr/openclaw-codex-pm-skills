# Governance Sandbox scenario-report entry note

Keep the first governance-sandbox slice small and replayable: one JSON/YAML scenario file in, one JSON/Markdown/HTML report bundle out.
Use this when PM copy needs a shortest-path reminder before widening presets, UI work, or browser proof.
