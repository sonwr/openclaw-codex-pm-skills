# OPENCLAW_PM_REPO45_SOURCE_PATH_ALIAS_NOTE

Use this when repo 4 and repo 5 need one compact phase-one reminder that `source_path` should stay in the same scenario-file proof lane as other governance-sandbox source aliases before the five-line validation/hold report.
