# Governance-sandbox repo 4 + 5 priority pair note

When the five-repo maintenance loop is active, always keep repo 4 (`oss-launchpad-cli`) and repo 5 (`governance-sandbox`) in the slice, and keep repo 5 scoped to scenario-file input plus markdown/html/json report proof before widening preset or web-demo work.
