# OPENCLAW_PM_REPO45_REPORT_OUTPUT_FILES_ALIAS_NOTE

Use this note when repo 4 should stay visibly active while repo 5 proves one scenario-file replay plus one generated report bundle whose artifact paths are exposed through `report.outputs.files`.

Keep the pass narrow:
- repo 4 still lands one validator-backed improvement in the same pass,
- repo 5 still stays scenario-file -> markdown/html/json report-bundle first,
- `report.outputs.files` is only another readable alias for the same generated bundle handoff before wider demo work.
