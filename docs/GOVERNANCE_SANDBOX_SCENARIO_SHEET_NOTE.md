# Governance sandbox scenario-sheet note

When the governance-sandbox roadmap expands scenario-file intake, keep wrapped fixtures narrow and reviewable.

- Prefer one wrapper key that still exposes proposal + stakeholder input clearly.
- Keep report-title metadata near the same wrapped scenario so generated memo output stays easy to audit.
- Treat new wrapper aliases as fixture ergonomics, not a replacement for stable report validation.
