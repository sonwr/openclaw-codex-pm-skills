# GOVERNANCE_SANDBOX_SCENARIO_JSON_YAML_PRIORITY_NOTE

Keep repo-1 PM wording aligned with repo-5's current delivery order:

- scenario file input still starts with JSON or YAML;
- the first believable output is the regenerated JSON / Markdown / HTML report bundle;
- presets and web-demo polish stay behind that validated phase-one pair.
