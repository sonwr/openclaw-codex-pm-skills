# governance-sandbox scenario/report active pair note

Keep the smallest believable repo-5 win paired as:

1. one imported JSON/YAML scenario file
2. one regenerated JSON/Markdown/HTML report bundle

Do not widen into preset or web-demo work until that pair is still replayable with a passing validation step.
