# Governance Sandbox scenario top-level wrapper note

Use a plain top-level `scenario` wrapper when one governance-sandbox fixture should keep proposal, stakeholders, and report metadata nested under a single key.

Keep the proof lane the same: one imported scenario file, one generated JSON / Markdown / HTML report bundle, one replayable handoff.
